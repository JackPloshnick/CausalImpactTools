% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_min_lift.R
\name{find_min_lift}
\alias{find_min_lift}
\title{Find the minimum lift needed for a CausalImpact Test}
\usage{
find_min_lift(
  data = NULL,
  pre.period = NULL,
  post.period = NULL,
  model.args = NULL,
  bsts.model = NULL,
  post.period.response = NULL,
  alpha = 0.05,
  per_period_lifts = NULL
)
}
\arguments{
\item{data}{A data frame or matrix used for the \code{CausalImpact} analysis.}

\item{pre.period}{A numeric vector specifying the start and end indices of the pre-intervention period.}

\item{post.period}{A numeric vector specifying the start and end indices of the post-intervention period.}

\item{model.args}{Arguments for the Bayesian Structural Time Series model passed to \code{CausalImpact}.}

\item{bsts.model}{A pre-constructed \code{bsts} model (optional). Defaults to \code{NULL}.}

\item{post.period.response}{Response data for the post-period (optional). Defaults to \code{NULL}.}

\item{alpha}{Significance level for the analysis. Defaults to \code{0.05}.}

\item{per_period_lifts}{A numeric vector of lift values per period.}
}
\value{
A data frame with two columns:
\itemize{
\item \code{lift}: The simulated lift values.
\item \code{p_value}: The corresponding p-values from the \code{CausalImpact} analysis.
}
}
\description{
This function simulates different lift values to determine the smallest lift detectable
by the \code{CausalImpact} package. It loops through the provided lift values and calculates
the p-value for each simulated lift.
}
\examples{
# Example usage:
library(CausalImpact)

x1 <- 100 + arima.sim(model = list(ar = 0.999), n = 100)
y <- 1.2 * x1 + rnorm(100)
data <- cbind(y, x1)
df <- find_min_lift(data, per_period_lifts = c(0.01,1,2), pre.period = c(1,70), post.period = c(71,100))
print(df)
}
